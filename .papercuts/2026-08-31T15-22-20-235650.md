---
time: 2026-08-31T15:22:20-05:00
branch: hermes-fork-patches
---

Ran the repository test suite with 'vp run -r test' as the AGENTS.md 'Verifying' section implies. It runs 10 workspace projects and silently leaves out apps/server (package name 't3') and apps/desktop, which together hold 3739 of the repository's tests. Nothing in the output says two projects were skipped, so a green result reads as a full pass. Reaching them needs a second command, 'vp run --filter t3 --filter @t3tools/desktop test', because --filter and --recursive cannot be combined. Fix: register the test task for those two projects in the recursive run, or name the two-command form in AGENTS.md.
