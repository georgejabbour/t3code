---
time: 2026-08-24T09:39:03-05:00
branch: hermes-fork-patches
---

Ran the T3 Code fork updater and found it had failed 178 checks in a row since 2026-08-18, every one with the single line 'the fork checkout has uncommitted changes'. The cause was one untracked .papercuts note file: t3code-fork-update uses 'git status --porcelain', which reports untracked files, so the papercut workflow (leave notes in the tree until a feature commit) permanently blocks the updater. The message never names the offending path, so the cause is invisible. Fix: make the gate ignore untracked .papercuts/ entries, or print the offending paths in the failure line.
